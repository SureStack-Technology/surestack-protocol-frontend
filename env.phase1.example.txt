# Phase 1 onboarding — environment variables
# Copy relevant keys into `.env.local` (frontend) and `backend/.env` (API).

# ===== Frontend (Vite) — `.env.local` =====
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
# Local dev: npm run dev serves http://localhost:3000 — add this origin in Clerk Dashboard.
# OAuth/email completion redirects to /auth/redirect (profile-aware routing).
# Optional: Clerk redirect overrides (otherwise ClerkProvider + SignUp/SignIn props apply)
# https://clerk.com/docs/guides/development/clerk-environment-variables#sign-in-and-sign-up-redirects
# VITE_CLERK_SIGN_UP_FORCE_REDIRECT_URL=http://localhost:3000/onboarding
# VITE_CLERK_SIGN_IN_FORCE_REDIRECT_URL=http://localhost:3000/dashboard
# Optional: override API (defaults to http://localhost:5001)
# VITE_BACKEND_URL=http://localhost:5001

# ===== Backend — `backend/.env` =====
PORT=5001

# Development only (NODE_ENV=development): force membership tier for internal testing.
# Does not persist to DB; ignored in production. Maps ALPHA_ACCESS/ENTERPRISE to effective tiers.
# DEV_FORCE_MEMBERSHIP_TIER=INTELLIGENCE_PRO
# Supported: EXPLORER | INTELLIGENCE_PRO | ALPHA_ACCESS | STRATEGIC_ACCESS | ENTERPRISE
DATABASE_URL=postgresql://USER:PASSWORD@localhost:5432/surestack?schema=public
CLERK_SECRET_KEY=sk_test_...
CLERK_WEBHOOK_SECRET=whsec_...
ALLOWED_ORIGINS=http://localhost:3001,http://127.0.0.1:3001

# Resend (transactional email)
RESEND_API_KEY=re_...
RESEND_FROM_EMAIL="SureStack <onboarding@yourdomain.com>"

# Wallet Risk Index (Ethereum mainnet MVP)
# Alchemy for balances, transfers, allowance probes
ALCHEMY_API_KEY=
# Optional: OpenAI narrator for risk summary (never used for scoring)
OPENAI_API_KEY=

# Optional: internal FastAPI narrator (falls back to OpenAI if unreachable)
INTERNAL_AI_SERVICE_URL=
INTERNAL_AI_SERVICE_TOKEN=

# Optional: ERC20 Approval log sweep window (alchemy_getLogs bounded scan)
WALLET_APPROVAL_LOG_LOOKBACK_BLOCKS=25000

# Universal Risk Scanner — Solana (mainnet RPC)
# Required in production; dev falls back to public mainnet-beta if unset
SOLANA_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY

# Token holder / liquidity intelligence (EVM + Solana via GoPlus token_security)
# DexScreener is used without a key. Optional: Etherscan for contract metadata (existing).
GOPLUS_APP_KEY=
# GOPLUS_API_KEY=  # alias supported

# Solana meme-token intelligence — free MVP uses DexScreener + Jupiter + RPC (no key required).
# Optional Birdeye enhancement when keyed:
# BIRDEYE_API_KEY=

